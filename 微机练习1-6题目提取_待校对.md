# 2026 微机原理及应用练习 1-6 题目提取版（待校对）

> 说明：以下内容根据截图整理。个别截图文字较模糊，我已用「【待校对】」标出不确定处。请先校对文字，确认后我再合并排版成 PDF。

---

## 练习 1

### 1. 单选题（1.5 分）

51 单片机的机器周期由 12 个时钟周期组成，如果采用 8MHz 晶振作为时钟源，则一条 4 机器周期指令的执行时间为：

A. 2us  
B. 3us  
C. 6us  
D. 8us

**正确答案：C**

---

### 2. 填空题（3 分）

根据温度传感器 AT30TSE752A 的数据输出规则，回答下面问题：

1. 传感器输出值是 `0xB530` 时，它表示的温度值是：____
2. 传感器测得的温度值是 `-32.1875℃` 时，其输出值的十六进制表示为：____

注意：填空中按需要写正负号（`+/-`）、数字和小数点，不要写表示进制类型的前后缀或下标！

**正确答案：**

- 填空 1：`-74.8125`
- 填空 2：`DFD0`

---

### 3. 填空题（5 分）

某 64 位计算机存储系统中有符号整数数据类型规定为：`char` 为 8 位、`short` 为 16 位、`int` 为 32 位、`long` 为 64 位，存储模式为小端模式。下面数据块的各个成员按顺序从零地址单元开始，按照边界对齐原则依次向高地址单元存储，请根据数据块在存储器中的映像，完成下面的填空。

数据块：

```c
int m1 = 285;
long m2 = -1020;
char m3 = -10;
int m4 = -129;
```

注意：若单元未被使用或内容不确定，填 `XX`；用十六进制表示，填空中只需写数字，不要加表示进制的前后缀或下标！

内存映像填空位置：

| 地址 | +0 | +1 | +2 | +3 | +4 | +5 | +6 | +7 |
|---|---|---|---|---|---|---|---|---|
| `$0000` | (1) |  | (2) |  |  | (3) |  |  |
| `$0008` |  | (4) | (5) |  | (6) |  |  |  |
| `$0010` | (7) |  |  | (8) | (9) |  | (10) |  |

**正确答案：**

- 填空 1：`1D`
- 填空 2：`00` / `0`
- 填空 3：`XX` / `X`
- 填空 4：`FC`
- 填空 5：`FF`
- 填空 6：`FF`
- 填空 7：`F6`
- 填空 8：`XX` / `X`
- 填空 9：`7F`
- 填空 10：`FF`

---

### 4. 填空题（4 分）

某二进制浮点数字长 20 位，其中阶码和尾数均用补码表示，阶码 7 位、尾数 13 位（均包含符号位），分别写出其所能表示的最大负数、最小规格化正数和最小非规格化正数。

（用十六进制表示，填空中只需写数字，不要加表示进制的前后缀或下标。）

1. 最大负数：____
2. 最小规格化正数：____
3. 最小非规格化正数：____

**正确答案：**

- 填空 1：`81FFF`
- 填空 2：`80800`
- 填空 3：`80001`

---

### 5. 单选题（1.5 分）

某计算机系统采用独立的 I/O 端口编址方式，I/O 端口空间的地址位数为 7，那么该计算机可访问的 I/O 端口数量为：

A. 7  
B. 32  
C. 64  
D. 128

**正确答案：D**

---

## 练习 2

### 1. 单选题（2 分）

ATmega256RFR2 片内程序存储器的容量为：

A. 128K  
B. 128KB  
C. 256K  
D. 512KB

**正确答案：A**

---

### 2. 单选题（1 分）

ATmega256RFR2 单片机中，通用寄存器 R31 在数据存储器空间的地址为：

A. `$00`  
B. `$1F`  
C. `$20`  
D. `$3F`

**正确答案：B**

---

### 3. 单选题（2 分）

ATmega256RFR2 芯片默认情况下 SP 的值是：

A. `$0000`  
B. `$2000`  
C. `$21FF`  
D. `$81FF`

**正确答案：D**

---

### 4. 单选题（1 分）

某计算机存储系统中，某一块内存的地址范围如下：`0x5000,0000 - 0x5001,9FFF`，请问该块内存容量是：

A. 32K  
B. 64K  
C. 104K  
D. 112K

**正确答案：C**

---

### 5. 单选题（2 分）

若 ATmega256RFR2 芯片使用外部 32MHz 晶振作为时钟源，其默认系统时钟频率为：

A. 4MHz  
B. 8MHz  
C. 16MHz  
D. 32MHz

**正确答案：A**

---

### 6. 单选题（1 分）

某计算机的时钟频率为 8MHz，已知某条指令执行的 Cycle Counter 为 48，则该条指令的执行时间为：

A. 2us  
B. 3us  
C. 4us  
D. 6us

**正确答案：D**

---

### 7. 单选题（1 分）

ATmega256RFR2 芯片默认的系统时钟频率为：

A. 32.768KHz  
B. 8MHz  
C. 1MHz  
D. 16MHz

**正确答案：C**

---

### 8. 单选题（2 分）

ATmega256RFR2 中 PC 的位宽是：

A. 18  
B. 17  
C. 16  
D. 8

**正确答案：B**

---

### 9. 单选题（1 分）

关于 AVR 单片机存储器结构，下列说法正确的是：

A. 小端模式的普林斯顿结构  
B. 大端模式的普林斯顿结构  
C. 小端模式的哈佛结构  
D. 大端模式的哈佛结构

**正确答案：C**

---

### 10. 填空题（2 分）

查看课件中“ATmega256RFR2 的 I/O 寄存器”表，请写寄存器 PORTB 的地址：

注意：用十六进制表示，直接填写数字即可，无需任何表示进制的前后缀。

1. PORTB 的统一编址地址：____
2. PORTB 的独立编址地址：____

**正确答案：**

- 填空 1：`25`
- 填空 2：`05` / `5`

---

## 练习 3

### 1. 判断题（1 分）

AVR 指令集中，两地址指令被执行后，目的操作数地址中原来存储的内容被覆盖，代之以运算或操作的结果，源操作数地址中的内容被清零。【待校对】

**正确答案：错误**

---

### 2. 单选题（1 分）

AVR 单片机中，使用标准 I/O 寻址方式寻址 I/O 寄存器时，地址范围是：【待校对】

A. `0x00 - 0x1F`  
B. `0x00 - 0x3F`  
C. `0x20 - 0x3F`  
D. `0x20 - 0x5F`

**正确答案：B**

---

### 3. 单选题（1 分）

AVR 单片机中，使用直接寻址方式寻址 I/O 寄存器时，地址范围是：

A. `0x00 - 0x1F`  
B. `0x00 - 0x3F`  
C. `0x20 - 0x3F`  
D. `0x20 - 0x5F`

**正确答案：D**

---

### 4. 单选题（1 分）

ATmega128 中，用直接寻址方式寻址数据空间时，指令中的地址位数是：【待校对】

A. 17  
B. 16  
C. 18  
D. 8

**正确答案：B**

---

### 5. 填空题（2 分）

要使得 `ELPM` 指令将 Flash 中地址为 `$A203` 的高字节存于寄存器中，若 `(Z) ← $____`，`(RAMPZ) ← $____`。【待校对】

**正确答案：**

- 填空 1：`4407`
- 填空 2：`01`

---

### 6. 单选题（1 分）

`LPM` 指令中的 `Z` 寄存器以字节地址从 Flash 存储器中读取一个字节的数据，下面指令段执行 `LPM` 指令读取的数据在 FLASH 中的地址为：【待校对】

```asm
ldi ZH, 0
ldi ZL, $ED
lpm
```

A. `0x01A6`  
B. `0x00ED`  
C. `0x006A`  
D. `0x00D3`

**正确答案：B**

---

### 7. 填空题（2 分）

AVR 单片机中，内存堆栈输出如下图所示，若 `SP = $30d5`，执行指令 `POP R0` 后：【待校对】

1. `(R0) = $____`
2. `(SP) = $____`

**正确答案：**

- 填空 1：`33`
- 填空 2：`30d6`

---

### 8. 填空题（8 分）

已知：`(R0)=0x23`，`(R1)=0x22`，`(R2)=0x23`，`(R3)=0x24`，分析下面汇编指令段：【待校对】

```asm
1. ldi R27, 0
2. ldi R26, $F6
3. st X, R0
4. ld R1, X+
5. ldi R26, $F5
6. st X, R2
7. st -X, R3
```

第 3 行中，源操作数是（1）____，寻址方式：（2）____；目的操作数是（3）____，寻址方式：（4）____。  
第 5 行中，源操作数是（5）____，寻址方式：（6）____。  
第 7 行指令中，`X = 0x`（7）____。  
第 7 行指令执行后，DATA RAM 内容映像为：

| 地址 | 内容 |
|---|---|
| `$00F3` |  |
| `$00F4` | `0x`（8） |
| `$00F5` | `0x`（9） |
| `$00F6` | `0x`（10） |

**正确答案：**

- 填空 1：`R0`
- 填空 2：`寄存器直接寻址`
- 填空 3：`X`
- 填空 4：`寄存器间接寻址`
- 填空 5：`F5`
- 填空 6：`立即数`
- 填空 7：`00F5`
- 填空 8：`22`
- 填空 9：`24`
- 填空 10：`23`

---

### 9. 多选题（2 分）

在 AVR 单片机的数据存储器寻址方式中，指针寄存器的内容会改变的是：____ 的间接寻址模式。【待校对】

A. 后增一  
B. 后减二  
C. 前减一  
D. 寄存器直接

**正确答案：A、C**

---

### 10. 多选题（2 分）

对于 Mega128 单片机片内 SRAM 中 `0x0060 - 0x00FF` 地址范围内的存储单元，可使用的寻址方式有：

A. 立即数寻址  
B. 直接寻址  
C. 间接寻址  
D. 寄存器寻址

**正确答案：B、C**

---

### 11. 多选题（2 分）

AVR 8 位算术指令集中，按照地址码字段中地址的数目，有如下格式的指令：

A. 零地址指令  
B. 一地址指令  
C. 两地址指令  
D. 三地址指令

**正确答案：A、B、C**

---

## 练习 4

### 1. 填空题（25 分）

已知 `time_buff = $0421`，`display_buff = $1005`，且从 `$0421` 开始的 SRAM 内存单元中存放的数据为：`$09, $27, $31, $45`，分析程序并填空。

```asm
1  put:  ldi xl, low(time_buff)
2        ldi xh, high(time_buff)
3        ldi yl, low(display_buff)
4        ldi yh, high(display_buff)
5        ldi r18, 4
6  loop: ld  r23, x+
7        mov r24, r23
8        swap r24
9        andi r24, 0x0f
10       andi r23, 0x0f
11       st  y+, r23
12       st  y+, r24
13       dec r18
14       brne loop
15       ret
```

填空：

1. 第 6 行第一次执行后，`X = $____`
2. 第 6 行第一次执行后，`R23 = $____`
3. 第 8 行第一次执行后，`R24 = $____`
4. 第 9 行第一次执行后，`R24 = $____`
5. 第 10 行第一次执行后，`R23 = $____`
6. 第 12 行第一次执行后，`Y = $____`
7. 子程序执行过程中，`loop` 循环的次数 = ____

子程序执行完后，DATA Memory 存储器的映像为：

| 地址 | 内容 |
|---|---|
| `$100C` | `$`（8） |
| `$100B` | `$`（9） |
| `$100A` | `$`（10） |
| `$1009` | `$`（11） |
| `$1008` | `$`（12） |
| `$1007` | `$`（13） |
| `$1006` | `$`（14） |
| `$1005` | `$`（15） |

**正确答案：**

- 填空 1：`0422` / `422`
- 填空 2：`09` / `9`
- 填空 3：`90`
- 填空 4：`00` / `0`
- 填空 5：`09` / `9`
- 填空 6：`1007`
- 填空 7：`4`
- 填空 8：`04` / `4`
- 填空 9：`05` / `5`
- 填空 10：`03` / `3`
- 填空 11：`01` / `1`
- 填空 12：`02` / `2`
- 填空 13：`07` / `7`
- 填空 14：`00` / `0`
- 填空 15：`09` / `9`

---

## 练习 5

### 1. 填空题（20 分）

已知 DATA RAM 内存中地址 `$8020` 开始的若干内存单元中的数据依次为：`13, 4, 61, 87, 255, 124, 37, 132`；代码运行前，所有寄存器初值均为 0，试分析如下汇编指令代码的执行过程：

```asm
1  L0: ldi r17, 8
2      ldi XL, $20
3      ldi XH, $80
4  L1: ld  r20, X+
5      ldi r18, 7
6  L2: lsr r20
7      brcs L3
8      inc r19
9  L3: dec r18
10     brne L2
11     dec r17
12     brne L1
13     ret
```

填空：

1. 第 4 行第一次执行后，`(X) = $____`；
2. 第 4 行第一次执行后，`(r20) = $____`；
3. 第 6 行第一次执行后，`(r20) = $____`；
4. 第 11 行第一次执行后，`(r19) = $____`；
5. 执行到第 13 行时，`(r19) = $____`；
6. 执行到第 13 行时，`(r20) = $____`；
7. 执行到第 13 行时，`(r26) = $____`；
8. 执行到第 13 行时，`(r27) = $____`；
9. 全部代码执行完成后，`L1` 的循环次数 = ____；
10. 全部代码执行完成后，`L2` 的循环次数 = ____。

**正确答案：**

- 填空 1：`8021`
- 填空 2：`0D` / `d` / `13`
- 填空 3：`06` / `6`
- 填空 4：`04` / `4`
- 填空 5：`1A` / `26`
- 填空 6：`01` / `1`
- 填空 7：`28`
- 填空 8：`80`
- 填空 9：`8` / `08`
- 填空 10：`56` / `38`

---

## 练习 6

### 1. 填空题（20 分）

分析下面汇编程序代码，填空回答问题。

```asm
1  su1: ldi r31, high(t1<<1)
2       ldi r30, low(t1<<1)
3       ldi r29, $03
4       mov r28, r30
5  d0:  lpm r20, Z+
6       andi r20, $FE
7       st Y+, r20
8       subi r30, -1
9       cpi r30, $00
10      brne d0
11      ret
12 .org $0941
13 t1:  .dw $0011, $2233, $4455, $6677
14      .dw $8899, $aabb, $ccdd, $eeff
```

填空：

1. 第 4 行执行后，`(Z) = $____`；
2. 第 4 行执行后，`(Y) = $____`；
3. 第 5 行第一次执行后，`(r20) = $____`；
4. 第 5 行第一次执行后，`(Z) = $____`；
5. 第 7 行第一次执行后，`(Y) = $____`；
6. 第 8 行第一次执行后，`(r30) = $____`；

第 11 行执行后，DATA memory 映像为：

| 地址 | 内容 |
|---|---|
| `$0385` | `$`（7） |
| `$0386` | `$`（8） |
| `$0387` | `$`（9） |
| `$0388` | `$`（10） |

**正确答案：**

- 填空 1：`1282`
- 填空 2：`0382`
- 填空 3：`11`
- 填空 4：`1283`
- 填空 5：`0383`
- 填空 6：`84`
- 填空 7：`76`
- 填空 8：`98`
- 填空 9：`BA`
- 填空 10：`DC`
